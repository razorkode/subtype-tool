# AWS SES Configuration
# Copy this file to .env and fill in your values
# (You may need to configure your PHP to load .env files, or set these as actual environment variables)

# AWS Region (e.g., us-east-1, eu-west-1)
AWS_REGION=us-east-1

# AWS Credentials
# These can also be set via ~/.aws/credentials file
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Sender Email (must be verified in SES)
SES_SENDER_EMAIL=noreply@yourdomain.com
SES_SENDER_NAME=Subtype Tool
